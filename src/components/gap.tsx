import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
//@ts-ignore
const Gap = ({ height }) => {
    return (
        <View style={{
            height: height,
        }} />
    )
}

export default Gap;
const styles = StyleSheet.create({})